apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-deploy.fullname" . }}
  labels:
    {{- include "custom-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "custom-deploy.selectorLabels" . | nindent 4 }}
